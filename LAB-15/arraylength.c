#include<stdio.h>
#include<string.h>

void main(){
    char a[100];
    printf("Enter the string = ");
    gets(a);

    strlen(a);
    printf("Enter the length of string %d",strlen(a) );

}